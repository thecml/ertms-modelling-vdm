class Point

instance variables
	private x : nat;
	private y : nat;
	
operations
	public GetCoord:() ==> nat * nat
	GetCoord() ==
		return mk_(x,y);
	
	public Point: nat * nat ==> Point
	Point(px, py) ==
	(
		x := px;
		y := py;
	);
		
end Point